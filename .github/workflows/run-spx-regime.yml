name: Run SPX Regime Analysis

on:
  workflow_dispatch:  # Allow manual triggering
  schedule:
    - cron: '30 22 * * 1-5'  # 22:30 UTC Mon–Fri ((6:30 PM ET during DST, 5:30 PM ET during Standard))

permissions:
  contents: write

jobs:
  update-analysis:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
        
    - name: Set up Conda
      uses: conda-incubator/setup-miniconda@v3
      with:
        auto-update-conda: true
        python-version: '3.12'
        
    - name: Create conda environment
      shell: bash -l {0}
      run: |
        # Create a new environment with core dependencies
        conda create -n pynance-v2.0 python=3.12 -y
        
        # Install core packages via conda
        conda install -n pynance-v2.0 -c conda-forge -c anaconda \
          pandas numpy matplotlib jupyter ipython \
          requests beautifulsoup4 lxml pyyaml pip anthropic -y
        
        # Install additional packages via pip
        conda activate pynance-v2.0
        pip install tradingview-datafeed yfinance

    - name: Restore history from gh-pages
      run: |
        git fetch origin gh-pages --depth=1 || true
        git checkout origin/gh-pages -- history/allocation-log.jsonl 2>/dev/null && \
          mkdir -p docs/history && \
          mv history/allocation-log.jsonl docs/history/allocation-log.jsonl && \
          rm -rf history/ || true

    - name: Restore data cache from gh-pages
      run: |
        git checkout origin/gh-pages -- data-cache/ 2>/dev/null && \
          cp data-cache/*.csv data/ && \
          rm -rf data-cache/ || true

    - name: Activate conda environment and run analysis
      shell: bash -l {0}
      run: |
        conda activate pynance-v2.0
        python data/fetch_data.py
        python research/combined-research.py
        python allocation/allocation_engine.py
      env:
        TRADINGVIEW_USERNAME: ${{ secrets.TRADINGVIEW_USERNAME }}
        TRADINGVIEW_PASSWORD: ${{ secrets.TRADINGVIEW_PASSWORD }}
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        
    # - name: Commit and push changes
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   run: |
    #     git config --local user.email "action@github.com"
    #     git config --local user.name "GitHub Action"
    #     git add docs/spx-regime.png docs/spx-regime-results.json
    #     git diff --quiet && git diff --staged --quiet || git commit -m "Update SPX regime analysis [skip ci]"
    #     git push origin gh-pages

    - name: Prepare data cache for deploy
      run: |
        mkdir -p docs/data-cache
        cp data/*.csv docs/data-cache/ 2>/dev/null || true

    # Publish output files (PNG + JSON) to gh-pages without history
    - name: Deploy to gh-pages (force orphan)
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./docs
        publish_branch: gh-pages
        force_orphan: true